``` C++
#include <iostream>
#unclude <math.h>

using std::cout;
using std::cin;

int main() {
	setlocate(LC_ALL, "Russian")
	
	float x;
	cout << "Введите x: "
	cin >> x;
	
	double y = exp(x) + log(abs(x)) + sqrt((pow)x, 2) + i));
	double y_rounded = round(y * 100000.0) / 100000.0;
	cout << "Результат" = " << y_rounded << endl;
}
```
