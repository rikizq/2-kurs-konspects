```C++
#include <iostream>
using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    int a, b, sum = 0;
    cout << "Введите число: ";
    cin >> a >> b;

    for (int i = a; i <= b; i++)
        sum += i;

    cout << sum;
}
```